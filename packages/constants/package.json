{
  "name": "@ervum/constants",
  "version": "1.0.0",
  "description": "Shared client/server constants",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf ./dist",
    "watch": "tsc -b --watch"
  },
  "dependencies": {
    "@ervum/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "npm-watch": "^0.13.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.9.2"
  }
}
