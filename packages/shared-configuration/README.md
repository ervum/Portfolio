# @ervum/shared-configuration

Shared configuration values for client and server (local development). Contains constants used by `portfolio-client` and `portfolio-server`.

Build with `pnpm build` from the package root or `pnpm -w build` from repository root.
