// Copy this file to `secrets.ts` within the same directory and fill in values for local development.
import { SecretsType } from '@ervum/types';

import { DefaultSecrets } from './index.js';



export const Secrets: SecretsType = {
    Database: {
        ... DefaultSecrets.Database
    }
};
